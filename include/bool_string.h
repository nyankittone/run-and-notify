// Macro that simply takes a boolean, and evaluates it to either the string "true" or "false".
// Useful inside of printf statements when you bwant to print a boolean value.
#define mBoolStr(the_bool) ((the_bool) ? "true" : "false")

